{
  "name": "network-children-server",
  "version": "1.0.0",
  "description": "The children server that connects and sends data back to the parent server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DIBSTER",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "prettysize": "^2.0.0",
    "systeminformation": "^5.12.6"
  }
}
